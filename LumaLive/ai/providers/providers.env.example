# Provider configuration example. Values are placeholders only.
LUMALIVE_AI_PROVIDER=deterministic
LUMALIVE_AI_MODEL=
LUMALIVE_AI_ENDPOINT=
LUMALIVE_AI_API_KEY_ENV=
# Production deployments should inject the actual secret through the OS/container secret manager.
